import React from 'react';

const Header = () => {
    return (
        <div style={{fontSize: "16px", marginBottom: '30px'}}>
            <h1>devfinder</h1>
        </div>
    );
};

export default Header;